<!-- 이 파일은 워크스페이스 전역 규칙 및 가드레일을 정의합니다. -->
<!-- AI 어시스턴트가 프로젝트 작업을 수행할 때 반드시 준수해야 하는 행동 지침을 포함합니다. -->

# 워크스페이스 개발 및 운영 규칙

## 1. 렌더링 실행 제약 규칙 (Rendering Guardrail)

- **원칙**: 카드뉴스 이미지 렌더링(`modules/card_renderer.py`, Playwright 캡처) 및 비디오 렌더링(`modules/video_renderer.py`, MoviePy/FFmpeg 인코딩)은 **사용자가 명시적으로 렌더링을 요청한 경우에만 실행**합니다.
- **허용 범위**:
  - 뉴스 수집, 기사 분석, 대본 생성, 텍스트 검수, 메타데이터 생성, 단위 테스트 등은 사용자의 일반 요청 시 자동 수행 가능합니다.
- **제한 범위**:
  - 사용자가 명시적으로 "렌더링해줘", "카드뉴스 뽑아줘", "영상 만들어줘", "쇼츠 렌더링 진행해" 등으로 지시하기 전에는 렌더링 단계(이미지/영상 파일 생성)를 임의로 실행하지 않습니다.
  - 전체 파이프라인(`pipeline.py`) 실행이 필요한 경우에도 렌더링 진행 여부를 사용자에게 먼저 확인하거나, 사용자의 렌더링 요청이 있을 때만 렌더링 단계를 가동합니다.

## 2. 특정 요일 부분 작업 처리 지침 (Granular Execution Guideline)

- **원칙**: 사용자가 "목요일 것만 대본 뽑아줘", "목요일 카드뉴스만 렌더링해줘", "목요일 것만 업로드해줘" 등 특정 요일을 지정하여 요청한 경우, **전체 6일을 무의미하게 재실행하지 않고 반드시 `--day <day>` 인자를 활용하여 해당 요일만 핀포인트로 실행**합니다.
- **주요 실행 매핑**:
  - 특정 요일 빌드/대본 생성: `python curate.py build --day thu`
  - 특정 요일 미디어 렌더링: `python curate.py build --day thu --render` (사용자의 명시적 렌더링 요청 시에만)
  - 특정 요일 SNS 업로드: `python curate.py upload --day thu` (테스트 시 `--dry-run`)
- **지원 요일 키워드**:
  - 영문: `mon`, `tue`, `wed`, `thu`, `fri`, `sat`
  - 한글: `월`, `화`, `수`, `목`, `금`, `토`, `월요일` 등
  - 번호: `01` ~ `06` 또는 `1` ~ `6`

## 3. 주간 큐레이션 파이프라인 개시 및 사용자 큐레이션 보장 규칙 (Curation Initiation Rule)

- **의도 해석 원칙**: 사용자가 **"컨텐츠 만들어줘"**, **"이번주 컨텐츠 만들어줘"**, **"큐레이션 시작해"** 등으로 발화하는 것은 전체 과정을 전자동으로 끝까지 수행하라는 의미가 아니라, **주간 큐레이션 파이프라인의 1단계(`python curate.py fetch`)를 시작하자는 신호**로만 해석합니다.
- **사용자 선별(큐레이션) 권한 절대 보장 (Human-in-the-Loop)**:
  - 기사 후보 대량 수집(`fetch`), 본문 크롤링(`review`), 패키지/대본 생성(`build`) 등 기술적 보조 작업은 AI가 수행합니다.
  - **어떤 기사를 최종 선택할지 결정(`[ ]` -> `[x]` 체크)하는 행위는 반드시 사용자(인간 편집자)의 고유 권한**입니다. AI가 임의로 기사 체크박스를 대신 체크하여 다음 단계로 자동 진행해서는 안 됩니다.
- **단계별 엄격한 실행 흐름 (5단계 라이프사이클)**:
  1. **"컨텐츠 만들어줘" 요청 시**:
     - `python curate.py fetch`만 실행합니다.
     - 완료 후 생성된 `candidates_titles.md` 파일 경로를 안내하며, *"관심 기사에 [x] 표시를 직접 진행해 주세요"*라고 안내한 뒤 **즉시 작업을 멈추고 대기**합니다.
  2. **사용자가 기사 1차 선별 후 "리뷰해줘", "크롤링해줘" 등 요청 시**:
     - `python curate.py review`를 실행하고, `candidates_detail.md` 파일 검토를 안내한 뒤 멈춥니다.
  3. **사용자가 최종 기사 확정 후 "빌드해줘", "패키지 만들어줘" 등 요청 시**:
     - `python curate.py build`를 실행합니다. (미디어 렌더링 없이 대본/패키지만 생성)
  4. **사용자가 "렌더링해줘", "영상 만들어줘" 등 명시적 요청 시**:
     - `python curate.py build --render`를 실행합니다.
  5. **렌더링 완료 후 (로컬 주간 파이프라인의 종착역)**:
     - 다음 권유 가능한 유일한 단계는 **GitHub 동기화(`python curate.py sync`)**뿐입니다.
     - "업로드 진행할까요?" 등 발행 관련 발화는 원천 금지되며, 오직 *"동기화를 진행할까요?"*만 안내합니다.
     - 사용자가 동기화 요청 시 `python curate.py sync`를 실행하고 **주간 파이프라인을 완전히 종료**합니다.

## 4. SNS 자동 발행 및 GitHub Actions 무인 스케줄링 준수 규칙 (Publishing Guardrail)

- **원칙**: 주간 큐레이션 및 미디어 렌더링이 완료되면 결과물은 GitHub 원격 저장소에 동기화(`python curate.py sync`)하는 것으로 주간 작업을 마무리합니다. 실제 SNS 발행은 **매일 아침 KST 08:00에 GitHub Actions(`daily_sns_publish.yml`)가 당일 요일 콘텐츠 1건만 자동으로 무인 공개(`upload --today-only`)**하는 것이 유일한 정규 발행 경로입니다.
- **물리적 차단 (Code Guardrail)**:
  - `curate.py upload`는 로컬 PC 환경(`GITHUB_ACTIONS != true`)에서 실제 API 발행이 코드 레벨에서 강제 차단(`sys.exit(1)`)되어 있습니다. 로컬에서는 오직 시뮬레이션(`--dry-run`)만 가능합니다.
- **금지 행위 (Strict Guardrails)**:
  - AI가 사용자에게 "전체 업로드 진행", "주간 6일 일괄 업로드" 등을 임의로 제안하거나 유도해서는 절대 안 됩니다.
  - 미래 요일(월~토)의 콘텐츠를 주말이나 평일 로컬 환경에서 한꺼번에 실시간 즉시 업로드하여 SNS 피드를 도배해서는 안 됩니다.

## 5. 플래닝 모드 필수 적용 규칙 (Mandatory Planning Mode Guardrail)

- **원칙**: 기능 추가, 로직 수정, 버그 패치 등 모든 개발 및 코드 변경 작업 시, AI는 소스 코드를 임의로 바로 수정하지 않고 **반드시 플래닝 모드(`implementation_plan.md`)를 먼저 가동**합니다.
- **엄격한 실행 절차**:
  1. **계획서 작성 (1단계)**: 변경 대상 파일, 수정 상세 내용(Diff/로직 설계), 검증 방안을 담은 `implementation_plan.md` 아티팩트를 작성하고, 사용자 승인(`request_feedback=True`)을 요청합니다.
  2. **작업 즉시 중단 및 대기**: 계획서 작성 후 즉시 응답을 종료하고 멈춥니다. 사용자가 UI의 **[Proceed]** 버튼을 누르거나 채팅으로 **"진행해"**, **"적용해"**, **"개발해"** 등의 명시적 승인을 내릴 때까지 코드를 1줄도 건드리지 않습니다.
  3. **승인 후 실행 (2단계)**: 사용자의 명시적 승인이 확인된 후에만 비로소 계획서에 정의된 파일 수정 및 명령어 실행을 진행합니다.
